# 论文笔记

个人论文阅读笔记，全部为 Markdown 文件，格式：`YYYY-MM-DD_作者_标题.md`。

- 所有笔记位于本仓库 `docs/` 目录。
- push 到 `main` 分支后，GitHub Actions 自动将 md 构建为 HTML 并发布到 GitHub Pages。
- 新增笔记：在 `docs/` 下添加 md 文件并提交即可，站点会自动更新。

## 索引

<!-- INDEX-BEGIN -->
<!-- INDEX-END -->

_表格由构建时自动生成（见 `hooks.py`），按文章日期倒序排列。_
